<%= simple_form_for [@course, @course_review] do |f|%>
<%= f.input :comment %>
<%= f.input :rating %>
<%= f.hidden_field :course, value: @course.id %>
<%= f.button :submit, label:'send review' %>
<% end %>